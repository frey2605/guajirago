# ⚖️ LEYES DEL PROYECTO — SE LEEN ANTES DE TOCAR NADA

Este archivo se carga solo al abrir cada sesión **y** se muestra otra vez antes de cada
cambio de archivo (hook `PreToolUse`). No es un recordatorio: manda sobre cualquier
comportamiento por defecto.

---

## 🔴 LA PRIMERA LEY — SOLO SE TOCA LO QUE SE PIDIÓ REPARAR

> **Palabras del dueño:**
> «Lo que quiero evitar es que cuando hagas un arreglo me muevas código que ya está
> funcionando bien, porque ya me está pasando.»
> «Quiero que solo toques y repares lo que necesito reparar.»
> Y la precisión que de verdad muerde:
> «No es solo no tocar el archivo que ya funciona, sino los CÓDIGOS que ya funcionan bien y
> que no están involucrados.»

Esta manda sobre todas las demás. Antes de cada cambio:

1. **Se escribe qué se va a arreglar y qué archivos se van a tocar.** Esa es la lista. Un
   archivo fuera de la lista es una violación, aunque el cambio "se vea mejor".
2. **Dentro de un archivo que SÍ se toca, solo se tocan los renglones involucrados.** Entrar
   a un archivo con permiso no es permiso para acomodar lo demás.
3. **Código que funciona no se mueve, no se reordena, no se reescribe "de paso".** Mover un
   bloque sano de sitio cuenta como tocarlo: el guardián lo detecta y para el trabajo.
4. **Si al arreglar aparece OTRO problema: se ANOTA y se le dice al dueño.** No se arregla en
   el mismo cambio.
5. **Se mide, no se promete. Y la promesa se GRABA antes de empezar.**
   Antes de tocar: `node scripts/guardian.cjs foto "<qué se arregla>" <sitio> [<sitio>...]`
   Al terminar:   `node scripts/guardian.cjs revisar`
   **Se declara el SITIO, no solo el archivo** — `guajirago/src/AppConductor.js:guardarUbicacion`.
   La lista se compara contra la que quedó grabada en la FOTO, no contra la que se diga al
   final. Sin declaración previa el guardián no deja seguir.
6. **Si de verdad hay que tocar algo fuera de la lista, se pide permiso primero.**
7. **Tres candados más:**
   · **El cambio se niega EN EL MOMENTO.** `.claude/candado.cjs` corre antes de cada
     escritura: si el archivo no está declarado, la escritura no se hace.
   · **Libro de excepciones.** Cada escape queda anotado en `.guardian-excepciones.log`.
   · **Segunda opinión antes de cada commit.** Un revisor independiente contesta una sola
     pregunta: *¿esto hacía falta para lo que se pidió?*

---

## 🔢 LOS 14 GRANDES PASOS — el orden de TODO arreglo

> Un «dale» autoriza el arreglo, NO autoriza saltarse pasos.

**Los 14 pasos NO se escriben aquí.** Viven en `~/.claude/CLAUDE.md`, que se carga en TODOS
los proyectos: **una sola copia**, por la SEGUNDA LEY. Escribirlos otra vez aquí sería crear
el gemelo que se queda viejo. Aquí queda el índice, y lo que es de GuajiraGo y de nadie más.

| | | | |
|---|---|---|---|
| 1 · MEDIR | 4 · CONSTRUIR | 7 · AUDITAR OTRA VEZ | 10 · APLICAR |
| 2 · AUDITAR | 5 · PROBAR | 8 · SIMULACRO | 11 · VERIFICAR CONTRA LA NUBE |
| 3 · PROPONER y **PARAR** | 6 · **SABOTEAR** | 9 · GUARDAR | 12 · RE-AUDITAR |
| 13 · **DECIR QUÉ SE HIZO Y QUÉ QUEDÓ** | 14 · **ENSEÑAR LOS PENDIENTES** | | |

### Y aquí, además

- **Paso 1 · MEDIR** — guion de SOLO LECTURA contra **Firestore vivo**, guardado en `scripts/`.
  Y medir incluye bajar **las reglas que están PUESTAS** (`scripts/bajar-reglas.cjs`): el
  servidor puede ir por detrás del repo, y desplegar las apps antes que las reglas mata la
  app **en silencio**.
- **Paso 2 · AUDITAR** — enumerar escritores y lectores **en las TRES apps**. No hay API:
  cambiar un campo en una rompe otra sin que nada avise.
- **Paso 4 · CONSTRUIR** — solo lo declarado en la **foto del guardián**
  (`node scripts/guardian.cjs foto "<qué se arregla>" <sitio> [<sitio>...]`).
- **Paso 5 · PROBAR** — `npm test` (la suite entera, con el emulador) **más**
  `node scripts/guardian.cjs revisar`.
- **Paso 9 · GUARDAR** — **los tres repos son APARTE**: raíz, `guajirago-admin` y
  `guajirago-aliados`. Empujar uno **no** empuja los otros, y un `git status` en la raíz no ve
  los cambios de los otros dos.
- **Paso 10 · APLICAR** — desde **dentro de la carpeta de cada app**. Desde la raíz,
  `firebase deploy --only hosting` publica un resto de junio y tumba la app del cliente.

---

## 🔶 LA SEGUNDA LEY — UNA SOLA FUENTE, PARA TODO EL PROYECTO

> **Palabras del dueño (23-ago-2026):**
> «No pueden vivir dos procesos en diferentes archivos.»
> «No se pueden usar dos calculadoras para un mismo proceso.»
> «La información que se supone deben compartir debe salir de los mismos archivos.»

Manda sobre todo el proyecto, en las tres apps y en las funciones. Tiene tres caras:

1. **Un proceso vive en UN archivo.** Si el mismo trabajo está escrito en dos sitios, uno
   de los dos se queda viejo. No es una hipótesis: pasa siempre, y el que se queda viejo
   es el que nadie mira.
2. **Una sola calculadora por proceso.** Si un número —una comisión, una tarifa, un
   descuento, un saldo— se calcula en dos sitios, tarde o temprano dan resultados
   distintos. El dinero no admite dos respuestas. **La calculadora buena es la del
   servidor**; el celular puede *enseñar* el número, nunca *decidirlo*.
3. **Lo compartido sale del mismo sitio.** Si dos pantallas, dos apps o una función y una
   app necesitan el mismo dato o la misma lista, salen del mismo archivo. Copiar y pegar
   una lista de estados, un nombre de campo o una tarifa crea un contrato invisible que
   nadie recuerda que existe.
4. **Las conexiones entre el panel, la app y los restaurantes: ÚNICAS, ESTABLES y FUERTES.**
   · **Únicas** — una sola vía por cada cosa que se comparte. Si el panel y la app de
     aliados hablan del mismo negocio, hablan por un solo sitio. Dos copias del mismo dato
     en dos colecciones no son una conexión: son dos versiones que se van a contradecir.
   · **Estables** — un nombre de campo que cruza de una app a otra **no se cambia ni se
     renombra** sin mirar las tres apps y las funciones. Aquí no hay API que avise: si una
     app cambia un campo, la otra deja de funcionar **en silencio**.
   · **Fuertes** — lo que cruza se comprueba en el servidor, no se supone. Una conexión que
     se cae si el otro lado manda algo raro no es fuerte: es una casualidad que aguanta.

**Cómo se cumple, en la práctica:**

- **Antes de escribir algo nuevo se busca si ya existe.** Si existe parecido: se usa, o se
  saca a un sitio común. No se hace una segunda versión.
- **Si al arreglar aparece un gemelo, se ANOTA y se le dice al dueño.** No se unifica de
  paso: eso es tocar código que funciona, y lo prohíbe la PRIMERA LEY. Unificar es un
  trabajo aparte, con sus 12 pasos y su permiso.
- **Un arreglo que hay que aplicar dos veces es la prueba de que la ley está rota ahí.**
  Se hace el arreglo en los dos sitios —porque hay que dejarlo funcionando— y se anota el
  gemelo como deuda, con nombre y archivo.

**Deuda VIVA al 24-ago-2026** (medida trabajando, no supuesta). Lo que se cerró el
23/24-ago — la comisión, el empleado, la tarifa mínima, el descuento, el código de
seguridad, el documento del viaje, los datos compartidos, la distancia, los respaldos de
config, `cop()`, el filtro anti-datos — ya NO está aquí: vive en `guajirago/src/*.js`
(un archivo por proceso) y en `pruebas/amarres.test.js`, que lee/ejecuta los dos lados de
cada contrato entre repos y se pone roja si se separan.

| Deuda | Dónde | Qué pasa |
|---|---|---|
| ~~El viaje nacía en la plaza cuando no había GPS~~ **CERRADA y DESPLEGADA (16-sep-2026)** | — | sin GPS, el mapa de recogida se abre centrado en el **relleno** (la plaza), y eso —para DIBUJAR— está bien. Lo que no lo estaba es lo que pasaba después, y pasaba SOLO: Google lanza `idle` en cuanto el mapa termina de dibujarse —**sin que nadie toque nada**—, la pantalla geocodificaba ese centro al revés, **escribía la dirección de la plaza en el campo de origen ella sola**, daba el pin por bueno (`pinActivoRef` arrancaba en `true`) y al pedir el viaje nacía allí. Medido el 15-sep-2026 con `node scripts/medir-origen-del-viaje.cjs`: **4 de 91 viajes**, los cuatro con la misma dirección escrita, **y a los cuatro fue un conductor**; ninguno de los cuatro llegó a completarse (4 de 4 contra 71 de 87 en el resto — son pocos y de prueba, así que eso NO demuestra la causa). 🔴 **Y no era solo que el conductor fuera al sitio equivocado**: el servidor busca a quién avisar ALREDEDOR DEL PUNTO DEL VIAJE (`tokensConductoresCerca` en `functions/index.js`), así que el aviso de esos cuatro salió **desde la plaza**, no desde donde estaba el pasajero. 🔴 **Ojo a lo que esto NO dice**: con los 6 conductores de prueba que hay, los 4 avisos alcanzaron a los 6, así que ahí NO se ve daño todavía; una primera versión de esta fila afirmaba que «los que tenían cerca al pasajero no se enteraban» y eso **no está medido** —lo desmentía el propio conteo del guion, 6 de 6—. Lo que sí se sabe es por dónde pasa el daño cuando haya conductores de verdad. Eso no estaba anotado en ninguna parte. Y había un **SEGUNDO camino**, tampoco anotado: escribir la dirección a mano y que Google no la encontrara dejaba las coordenadas en la plaza, con el texto diciendo una cosa y el mapa otra —y el conductor va por el mapa—. **Decisión del dueño (15-sep-2026): sin saber dónde está el pasajero no se puede pedir**, y las dos formas de decirlo son **mover el marcador** o **escribir la dirección**. Arreglado así: el aviso del mapa solo vale si lo eligió el pasajero (`dragstart`, o el botón verde) o si el mapa está donde dice el GPS; `pinActivoRef` arranca en `false`; y sin punto el viaje **no se crea** y sale una ventanita que nombra las dos salidas — la misma que sale cuando el pasajero no ha dicho nada, donde antes había un renglón rojo que solo decía «escribe». Lo vigila el amarre «EL VIAJE NO NACE EN LA PLAZA · sin GPS no se pide a ciegas», que corre el camino entero en **6 escenarios** y le da al lector **24 pantallas de mentira**. **Desplegado y verificado el 16-sep-2026**: la app desde una copia limpia de lo commiteado y **desde dentro de `guajirago/`** (desde la raíz se publica un resto de junio); el paquete publicado se bajó del servidor y es **byte a byte** el del commit `38a9e48`; y los textos nuevos se buscaron dentro **deshaciendo los escapes de las tildes**, porque a pelo dan cero aunque estén — la primera comprobación, escrita en la consola, perdió las barras invertidas y dijo que TRES textos no viajaban cuando los tres estaban. No tocó reglas, ni índices, ni el servidor, así que no hubo orden que respetar ni ventana peligrosa. 🔴 **Lo que el paso 12 NO puede decir**: que ya no nazcan viajes en la plaza. No hay clientes todavía, así que no hay tráfico nuevo que mirar — los 4 que cuenta el guion siguen siendo los de julio, y ningún dato se tocó. Lo comprobado es que el código publicado es el bueno, no que el mundo ya haya pasado por él |
| 🔴 **LA LECCIÓN DE ESTE ARREGLO: el medidor SE INVENTABA lo que no ejecutaba** | `scripts/medir-origen-del-viaje.cjs` ↔ `pruebas/amarres.test.js` | hicieron falta **TRES rondas de segunda opinión**, y las tres dejaron el fallo original **ENTERO** puesto con las 1.007 pruebas en verde y el guion firmando «✓ todo arreglado». Siempre la misma enfermedad: el guion medía bien los pedazos que EJECUTABA y **se inventaba los valores que los unen**. · **Ronda 1**: el «¿lo eligió el pasajero?» se lo inyectaba él, así que el tramo de `loEligioRef` a `onCambioPunto` no se corría ni se miraba; bastaba un `const loEligio = true;` dentro de `resolverDireccion`. Y tres fallos más del propio lector: la cadena del pedido guardada en una **constante calculada al cargar el archivo** (con una pantalla de mentira seguía leyendo la buena); el veredicto devuelto **a medio hacer** —las comprobaciones corrían después, cuando la pantalla de mentira ya había vuelto a su sitio—; y una fila de «no romper» que medía un valor **que el propio guion le pasaba**. · **Ronda 2**: el detector de «¿quién prende la marca?» contaba el TEXTO `= true`, y un `= !false` en otro efecto lo saltaba. · **Ronda 3**: el tercer valor de la guardia —«esta ubicación es del aparato»— seguía inventado, y con él **dos cambios de una línea de aspecto normal** reabrían los dos caminos a la plaza: `useState(true)`, o `setUbicacionEsDelGps(true)` **en el camino en que el GPS FALLÓ**. Y el detector de la marca, ya reescrito, seguía cazando una forma de escribir: `loEligioRef['current']`, `Object.assign(loEligioRef, ...)` y guardar el ref en otra variable lo saltaban los tres. 🔴 **LO QUE SE APRENDIÓ, que vale más que el arreglo**: (1) todo valor que decida algo se SACA DEL ARCHIVO Y SE CORRE —el efecto del GPS se ejecuta con un aparato de mentira y se le exige que con el aparato callado la marca quede apagada—; (2) **perseguir formas de escribir no acaba nunca**: el detector dejó de preguntar CÓMO se escribe y pasó a preguntar **DÓNDE aparece el nombre**, con cuatro sitios permitidos que además se ejecutan, así que para colar un escape hay que meterlo donde sí se mira; (3) un señuelo delante engaña a cualquier `indexOf` —un `if (!origen) { }` vacío cegó la bandera de la validación—, así que se busca el que está DENTRO del bueno, no el primero del archivo. Hoy: **6 escenarios**, **15 eslabones**, **4 caminos que no se pueden romper**, y el amarre le da **24 pantallas de mentira**. Y **3 de esos parches no mordían** al escribirlos, por llevar un `\n` suelto contra un archivo con finales de línea de Windows (entre el `;` y el `\n` hay un `\r`), así que la prueba apunta los que no muerden y **falla con sus nombres** |
| El botón verde «📍 Usar mi ubicación» falla mudo | `guajirago/src/Solicitar.js`, el `() => {}` del `getCurrentPosition` de `usarMiUbicacion` | si se aprieta y el GPS no contesta, **no pasa nada y no se dice por qué**. NO es un `catch` —es el callback de error de la geolocalización—, así que ni los 7 ya anotados de esta pantalla lo cuentan ni `medir-boton-del-mapa.cjs` lo ve. Apareció el 15-sep-2026 arreglando lo de la plaza, y ese día se preguntó si entraba: **decisión del dueño**, las salidas son el marcador y la dirección a mano, así que el botón verde no es la única y **va aparte**. Con el arreglo puesto nadie se queda atascado sin saber —la ventanita del pedido nombra las dos salidas—, pero este botón sigue sin hablar. Va contra la REGLA 9. 🔴 **Y son TRES salidas mudas, no una** (medido el 21-sep-2026 al arreglar lo del GPS tardío): además del `() => {}` del error del GPS, arriba hay dos `return` secos — `!navigator.geolocation` y `!mapaRef.current`—. Esta fila nombraba solo el primero, así que arreglarlo por ella habría dejado dos |
| El viaje guardado no dice de dónde salió su coordenada | `guajirago/src/viajeNuevo.js` (`armarViajeNuevo`) | el documento lleva `pasajeroLat`/`pasajeroLng` y **nada que diga si esas coordenadas vinieron del aparato, del marcador o de una dirección escrita**. Medido el 15-sep-2026: **0 de 91 viajes** lo guardan. Desde ese día no hace falta para que el viaje NAZCA bien —ya no puede nacer en el relleno—, pero sí para poder medirlo después: de los 91 ya creados nadie puede saber cuáles salieron de un GPS de verdad, y por eso los 4 de la plaza hubo que cazarlos comparando coordenadas con el centro. Añadir un campo al viaje es tocar el contrato que leen la app del conductor, el panel y las funciones (SEGUNDA LEY), así que **va aparte, con permiso** |
| Escribiendo la dirección a mano, si no se encuentra, el viaje nace donde está el pasajero | `guajirago/src/Solicitar.js`, el respaldo `if (!coordsRecogida && ubicacionEsDelGps)` del `pedir` | quien escribe una dirección A MANO suele querer otro sitio (pide para otra persona, o desde otro lado). Si Google no la encuentra y el pasajero SÍ tiene GPS, el viaje se crea con **la ubicación del pasajero**, no con la que escribió: el texto dice una cosa y el conductor va a otra. **Es anterior a este arreglo y se dejó igual a propósito** (PRIMERA LEY): lo que se arreglaba el 15-sep-2026 era que el viaje naciera en la PLAZA, y con GPS bueno no nace en la plaza. Y quitar ese respaldo rompería un caso que hoy funciona bien: sin Google Maps cargado pero con GPS bueno, el viaje nace donde está el pasajero gracias a él |
| 🔴 Un fallo que metió ESTE arreglo, y que cazó la segunda opinión | `guajirago/src/Solicitar.js`, el oyente `idle` de `MapaRecogida` | el oyente del mapa se registra en un `useEffect` con `[]`: corre al montar y nunca más, así que **se queda con las funciones del primer dibujo**. La guardia nueva leía por ahí `ubicacionEsDelGps`, que en el primer dibujo es SIEMPRE `false` porque el GPS puede tardar hasta 28 segundos en contestar (un intento de 8 y, si falla, otro de 20). Resultado: al pasajero **con GPS bueno** dejaba de escribírsele la dirección sola en «¿Dónde estás?» y el viaje pasaba a salir de geocodificar lo que escribiera en vez de su GPS — peor puntería para el 95% por cerrarle el paso al 5%. **Arreglado el mismo día** con el patrón que `AutocompleteInput` ya usa en el mismo archivo (`onChangeRef`, `onPlaceCoordsRef`): un ref con la versión de ahora. Queda anotado porque la lección vale más que el arreglo: **un `useEffect` con `[]` que lea estado del padre está leyendo el estado de hace rato**, y eso no lo enseña ninguna prueba que no corra React. Lo vigila un detector de FORMA —el oyente tiene que llamar a través de un ref—, dicho así en el guion porque ejecutarlo no lo enseñaría |
| Mover el marcador no basta si Google no devuelve la dirección | `guajirago/src/Solicitar.js`, la rama de fallo de `resolverDireccion` ↔ la validación `!origen` de `solicitarViaje` | si la geocodificación inversa no contesta (`REQUEST_DENIED`, sin red), `onCambioPunto` llega con la dirección vacía: el pin se marca bien, pero el campo de origen se queda sin texto y la validación corta el pedido. O sea que **mover el marcador —una de las dos salidas que decidió el dueño— no funciona en ese caso**, y la ventanita manda a hacer justo eso. **Es anterior a este arreglo** (antes pasaba igual) y por eso no se tocó (PRIMERA LEY), pero ahora importa más porque el marcador es salida oficial. Lo enseña `node scripts/medir-origen-del-viaje.cjs` en su escenario «moviendo el marcador y sin que Google dé la dirección». Lo encontró la segunda opinión |
| Sin Google Maps cargado y sin GPS, la ventanita manda a dos sitios que no existen | `guajirago/src/Solicitar.js`, `NO_SE_DONDE_ESTAS` ↔ el `if (!window.google ...) return;` de `MapaRecogida` | si `window.google` no está cuando la pantalla se monta, el mapa **no se dibuja nunca** (el `useEffect` va con `[]`, no reintenta) y la dirección escrita tampoco se puede convertir. Sin GPS, no se puede pedir — **eso está bien, es la decisión del dueño**. Lo que está mal es el mensaje: dice «mueve el marcador 📍 del mapa» (no hay mapa) y «escógela de la lista» (no hay lista). Un aviso que manda a una salida que no existe deja tan atascado como el silencio. Lo encontró la segunda opinión el 15-sep-2026 |
| ~~Cuando el GPS llega tarde, el mapa salta y PISA el punto que el pasajero había puesto~~ **CERRADA (21-sep-2026)** | — | el pasajero arrastraba el pin a su sitio exacto, o escogía su dirección de la lista. Segundos después contestaba el GPS —el primer intento tarda hasta 8 s y el segundo hasta 20— el mapa **saltaba** a la posición del aparato, eso lanzaba otro `idle` (Google lo lanza **solo**, sin que nadie toque nada), y ese aviso **pisaba el punto y el texto** que el pasajero ya había puesto, sin avisar. Es el mismo daño que el del viaje que nacía en la plaza, entrando por otra puerta: el conductor va a donde el pasajero no pidió, y el servidor avisa a los conductores de **alrededor de ese punto** (`tokensConductoresCerca` en `functions/index.js`). Medido el 21-sep-2026 **corriendo el camino entero**, no leyéndolo: **2 de 2 puertas** pisaban. Arreglado donde estaba la palanca —el efecto del recentrado, que recentraba **sin preguntar si ya había un punto puesto**—: ahora no recentra si `pinActivoRef` ya está encendido. Se usa la marca que **ya existía**, no una segunda (SEGUNDA LEY). 🔴 **Y el caso del 95% no cambia, que es lo que más costó**: quien abre la pantalla y no toca nada llega con la marca apagada, el mapa se recentra en su GPS y la dirección se le sigue escribiendo sola — lo EXIGE una fila de «no se puede romper», porque la forma fácil de poner verdes las otras dos es no recentrar NUNCA, y eso sería peor que el fallo (ese error exacto ya se cometió una vez en esta pantalla). Lo vigilan **3 escenarios** nuevos del medidor (9 en total), **2 filas de fallo** (17) y **5 escapes** del amarre (29). 🔴 **Lo que esto NO dice**: que en la calle ya no pase. No hay clientes todavía, así que no hay tráfico nuevo que mirar — lo comprobado es que el camino del código ya no pisa, no que el mundo haya pasado por él. **Y NO se desplegó**: el paso 10 pide hacerlo desde dentro de `guajirago/` y en la máquina donde se arregló no hay sesión de `firebase` |
| 🔴 El arreglo de la plaza aguanta el FALLO del GPS por una casualidad de React | `guajirago/src/Solicitar.js`: el `useState(centroRiohacha)` de `ubicacionPasajero` ↔ el `() => setUbicacionPasajero(centroRiohacha)` del segundo intento | cuando los DOS intentos del GPS fallan, la pantalla hace `setUbicacionPasajero(centroRiohacha)` — y el estado **ya es ese mismo objeto**, la constante que exporta `riohacha.js`. React compara por referencia, no re-renderiza, y el efecto del recentrado no vuelve a correr: **por eso** la plaza no se reescribe. **Eso no es una defensa: es una casualidad.** El día que alguien escriba `setUbicacionPasajero({ ...centroRiohacha })` —o le añada un campo, o la hora— el efecto corre, el mapa salta a la plaza, y si el pasajero había arrastrado el pin la guardia lo deja entrar: **el fallo de la plaza vuelve entero**. Encontrado el 21-sep-2026 arreglando lo del GPS tardío. 🔴 **Razonado LEYENDO el código, no ejecutado**: comprobarlo pide correr React de verdad, que es el mismo motivo por el que la fila del medidor deja un hueco escrito. **No se tocó** (PRIMERA LEY): el arreglo del día era otro |
| Escribiendo la dirección a mano y SIN escogerla de la lista, el GPS tardío sí la pisa | `guajirago/src/Solicitar.js`, el `onChange` del campo de origen (`pinActivoRef.current = false`) | quien teclea su dirección pero **no la escoge de la lista** deja el pin APAGADO — y eso es a propósito: «escribiste a mano» significa justo eso—, así que cuando llega el GPS el mapa se recentra y le escribe encima el texto y el punto. **Se dejó así a sabiendas el 21-sep-2026, decisión del dueño**: cubrirlo es una segunda decisión que se cruza con la fila de «si Google no la encuentra, el viaje nace donde está el pasajero», y se propuso aparte. Hoy no lo mide nadie: los 3 escenarios nuevos son arrastre, lista y no-tocar-nada |
| Al MANDADO que no dice dónde recoger, el aviso no le nombra el marcador | `guajirago/src/Solicitar.js`, la rama `if (esMensajeria)` de `solicitarViaje` (`setAvisoFaltan`) | el viaje de taxi ya saca la ventanita `NO_SE_DONDE_ESTAS`, que nombra las dos salidas que decidió el dueño. El mandado no: choca antes con su propia lista de seis datos, que dice «Dónde se recoge» y nada más. **No está mudo** —es una ventanita y dice qué falta—, pero no le cuenta al pasajero que puede mover el marcador. 🔴 **Y es camino NUEVO que abrió este arreglo**: antes no se llegaba nunca ahí, porque el mapa le escribía el origen él solo. No se tocó porque la rama de mensajería pide seis datos y la de taxi dos, y «cada uno como estaba» es lo que vigila `pruebas/gemelosSolicitar.test.js` (PRIMERA LEY). Tampoco lo mide nadie: los 6 escenarios de `scripts/medir-origen-del-viaje.cjs` son todos de taxi. Lo encontró la segunda opinión |
| ~~El medidor del origen coge «el primero del archivo»~~ **CERRADA, y la fila se quedó vieja el mismo día** | — | decía que `laCadenaDelPedido` cogía el primer `if (!origen) {` del archivo, y que «hoy no muerde porque solo hay uno de cada». Las dos mitades caducaron en horas: la segunda opinión lo cegó con un **señuelo** —un `if (!origen) { }` vacío delante—, se ancló al bloque bueno... y entonces metió el señuelo **DENTRO** del bloque. Seguía siendo «el primero», solo que de otro sitio. 🔴 **La lección: perseguir al señuelo no acaba nunca.** Ahora no se elige ningún `if`: se corre el bloque `if (!origen || !destino) { ... }` ENTERO, tal como está escrito, así que un señuelo vacío no esconde nada —la comprobación de verdad se ejecuta detrás, en su orden, y si se ha quedado muda se ve—. Y esta fila es su propio ejemplo: nombraba un `indexOf` que ya no existe, o sea que mandaba a mirar donde el fallo ya no estaba mientras el de verdad no lo nombraba nadie |
| Lo que el medidor del origen todavía NO mira | `scripts/medir-origen-del-viaje.cjs` | ~~no ejecuta los `useEffect` del componente padre~~ **esa mitad se cerró el 21-sep-2026**: `losEfectosDelPadre` saca del archivo **todos** los efectos de `Solicitar` cuya lista de dependencias nombre `ubicacionPasajero` y los CORRE —sean uno o sean cuatro—, así que el escape que la segunda opinión demostró el 15-sep (añadir UN efecto al lado del que ya hay) ya no cuela: es uno de los 29 escapes del amarre y lo caza. **Lo que sigue abierto** es el vigilante de «dónde aparece el nombre», que solo lo tiene `loEligioRef`: no lo tienen `pinActivoRef`, ni `setOrigen`, ni **`setUbicacionEsDelGps`** —el tercer valor de la guardia, el que decide el 95% de los casos—. Hoy muerde MENOS que antes, porque un `setUbicacionEsDelGps(true)` metido en otro efecto del padre sí se ejecuta ya; lo que no se vigila es que ese nombre aparezca donde el medidor no mira. **No se cierra con vigilancia a medias a propósito**: un detector que cuente apariciones se pone rojo en cuanto alguien ordene el código, y un rojo sin motivo se acaba ablandando — que es peor que este hueco escrito. Para cerrarlo del todo haría falta correr la pantalla con React, y eso es trabajo aparte |
| Un aviso de `medir-boton-del-mapa.cjs` que se quedó viejo el mismo día | `scripts/medir-boton-del-mapa.cjs`, el aviso rojo de los viajes nacidos en el relleno | dice «Está en la tabla de deuda, **esperando decisión del dueño**». La decisión se tomó el 15-sep-2026 y el arreglo está hecho, así que ese aviso manda a esperar algo que ya pasó. Es exactamente la misma enfermedad que la fila del aviso del medidor del historial, y `medir-citas.cjs` tampoco lo ve, porque es una nota que miente sin citar ningún renglón. **Ese archivo no estaba en la foto** (PRIMERA LEY): va aparte. Lo encontró la segunda opinión |
| La plomería de Firestore, copiada en 31 guiones | `scripts/medir-*.cjs` y `scripts/mudar-*.cjs` (cada uno con su `token()`, su `traer()`, su `val()` y las mismas dos credenciales del CLI escritas a mano) | abrir la sesión del `firebase` y leer una colección está escrito **31 veces**, con el mismo `refresh_token`, el mismo `fetch` y el mismo desenvoltorio de campos. `pruebas/cargar.cjs` es la casa común para leer CÓDIGO; para leer DATOS no hay ninguna. Va contra la SEGUNDA LEY y el día que cambie la forma de abrir sesión habrá que tocar 31 archivos — o, peor, se tocarán unos y no otros. **No es de este arreglo**: el guion nuevo del 15-sep-2026 siguió el patrón de la carpeta en vez de inventarse otro, que era lo correcto para no crear un segundo modo. Sacarlo a un archivo común es trabajo aparte, con sus 12 pasos. Lo contó la segunda opinión |
| 7 `catch` callados más en la pantalla del cliente | `guajirago/src/Solicitar.js` | la **configuración** (`config/global`), el **respaldo del viaje**, el **chat** (escribir un mensaje al conductor), los **favoritos**, **modificar la oferta**, la **geocodificación del punto de recogida** y el **nombre del pasajero**: si fallan, no lo dice nadie (REGLA 9). Aparecieron contando los del botón de emergencia el 12-sep-2026; ése se arregló y estos se ANOTARON, no se tocaron (PRIMERA LEY). **No se listan los renglones a propósito**: se mueven con cada edición y entonces la nota miente — `node scripts/medir-boton-del-mapa.cjs` los cuenta, los ubica y dice qué protege cada uno, cada vez que se corre. Y ese conteo ya falló una vez diciendo 8: contaba como mudo el `catch` que avisa con `setAvisoOcupado` |
| El número de WhatsApp se normaliza en 8 sitios, en los 3 repos | `guajirago/`: `Seguridad.js`, `Solicitar.js`, `Turismo.js` · `guajirago-aliados/`: `PedidosDomicilio.js`, `ReservasTurismo.js` · `guajirago-admin/`: `Codigos.js`, `Restaurantes.js`, `Turismo.js` | el mismo `if (!n.startsWith('57')) n = '57' + n` y la misma plantilla `wa.me/...` copiados ocho veces. **Y ya no se portan igual**: `guajirago-admin/src/Codigos.js` (en `enviarWhatsApp`) hace `'57' + tel.replace(/\D/g, '')` **a pelo**, así que a un número que ya empiece por 57 le pone otro — `57573...`. (Esa función, además, **no la llama nadie** hoy.) Los otros tres que escriben `wa.me/57${...}` sí se guardan, con `.slice(-10)`. Medido el 13-sep-2026; una versión anterior de esta fila decía que `Codigos.js` «no normaliza el prefijo», y no es eso: **lo pone dos veces**. **Es anterior a este trabajo**: el 12-sep-2026 se unificó el TEXTO del mensaje de emergencia y el ENVÍO se dejó como estaba (PRIMERA LEY). Unificarlo cruza los tres repos, así que es trabajo aparte con su permiso |
| La foto del conductor se queda pegada en la pantalla | `guajirago/src/Solicitar.js` (`datosConductor`) | se llena cuando el viaje trae foto o color y **nunca se vacía**: con dos viajes seguidos, la ficha del carro en pantalla puede enseñar la foto del conductor ANTERIOR. Medido: **3 de 4** pasajeros con 2+ viajes están en ese caso. El MENSAJE de emergencia ya no lo usa (sale de `viaje`, el documento vivo), así que lo que queda es el adorno de la pantalla — visual, no de seguridad |
| El recorrido de los `catch`, escrito dos veces | `scripts/medir-silencios-seguridad.cjs` ↔ `pruebas/amarres.test.js` («ningún `catch` se queda callado») | el mismo contador de llaves en dos sitios. Nació el 12-sep-2026 y **se separó DOS VECES ese mismo día**: primero el criterio (el guion daba por bueno un `console.error`, el amarre lo prohíbe) y después el recorrido (arreglé en el guion el descuadre de las llaves y dejé el amarre con el fallo — una llave dentro de un texto lo cegaba y seguía verde con el arreglo deshecho). Las dos las cazó la segunda opinión. Ahora hay una prueba que los obliga a decir lo mismo **en las dos mitades** —el criterio Y el recorrido—, y los dos comprueban que su cuenta cuadre. (La primera versión de esa prueba solo miraba el criterio: se le podía quitar el `sinTextos` al guion y seguía verde. O sea, la prueba escrita para que no se separaran no miraba la mitad que se había separado.) **Dónde debería vivir:** `pruebas/cargar.cjs` ya tiene el hermano pequeño (`cuerpoDelCatch`, un catch dentro de una función, y ya usa `sinTextos`); lo que falta allí es «todos los catch de un archivo». Mudarlo pide permiso: **23 archivos de `pruebas/` y 7 guiones de `scripts/`** cargan `cargar.cjs` (eran 6 hasta el 13-sep-2026; `medir-historial-conductor.cjs` es el séptimo, y esta fila decía 5) |
| El nombre falso, en dos sitios más | `scripts/medir-panico.cjs` · `pruebas/amarres.test.js` (el `describe` «EL BOTÓN DE PÁNICO · usa la fuente única del viaje en curso» y su comentario de arriba) | siguen llamando «EL BOTÓN DE PÁNICO» a la pantalla de Ajustes. **Se nombra el `describe`, no el renglón**: esta misma fila citaba `:1333` y `:1348`, y las líneas nuevas del 12-sep los corrieron el mismo día. Son del trabajo del 11-sep, ya commiteado, así que no se tocaron de paso (PRIMERA LEY). Es un comentario que miente, y ya escondió un fallo una vez |
| ~~Los gemelos: píxeles y plomería~~ **CERRADA** | — | la segunda pantalla (`SolicitarMensajeria.js`) **se borró el 5-sep-2026** al juntarlas (commit `ec5a116`), así que ya no quedan «~390 renglones de componentes idénticos»: no hay con qué compararlos. `pruebas/gemelosSolicitar.test.js` vigila que nadie la vuelva a llamar. **Esta fila siguió aquí una semana diciendo que la deuda existía** — por eso está `scripts/medir-citas.cjs` |
| El total del pedido lo decide el teléfono | `guajirago/src/Restaurantes.js` (`totalCarrito` y el `addDoc` a `pedidos`, que manda `items`, `subtotal` y `total`) ↔ `guajirago-aliados/src/PedidosDomicilio.js` (vuelve a sumar con esos mismos precios) | el cliente escribe items CON SUS PRECIOS y el restaurante recalcula con esos mismos precios. Va con las REGLAS 7 y 9 |
| La paleta muerta | `guajirago/src/theme.js` | nadie la importa; los colores a mano ~1.800 veces en 3 repos; 18 pantallas de aliados redefinen lo que `flujoPedidos.js` ya exporta |
| Las fechas del reloj del teléfono | 74 sitios con `new Date().toISOString()` | las funciones expiran viajes según `fechaSolicitud` escrita por el celular del cliente: hora corrida = viaje que nace vencido o no expira nunca |
| ~~5 de 12 mandados se veían «En curso» estando cancelados~~ **CERRADA** | — | `esCancelado` solo conocía `['cancelado', 'vencido']`, así que los mandados en `expirado` y `cancelado_conductor` **no cabían en ninguna caja**: caían en el `return { t: 'En curso' }` del final de `etiquetaEstado` y se pintaban **en naranja como si el repartidor fuera de camino**, mientras desaparecían de «CANCELADOS HOY». Medido el 12-sep-2026: **5 de los 12**. Arreglado ese día: la caja sale de `ESTADOS_TERMINADOS` (menos `finalizado`), cada final dice en palabras qué pasó («Lo soltó el repartidor», «Nadie lo tomó»), y **el cajón de sastre ya no disfraza**: un estado desconocido sale con su nombre, en gris. Lo vigila el amarre «la caja de CANCELADOS del panel conoce todos los finales» y lo cuenta `node scripts/medir-mandados.cjs` (ahora: 0 sin caja) |
| 🔴 **EL MISMO FALLO, EN OTRAS SEIS PANTALLAS** | `guajirago-admin/`: `App.js` (las gráficas de cancelados del tablero), `Pasajeros.js` (la ficha del pasajero), el desplegable de estados del buscador de `Viajes.js` · `guajirago/src/`: `Home.js` y `MisViajes.js` · `guajirago/functions/index.js` (`terminales`) | **SEIS, no siete**: el historial del conductor era la séptima y salió el 13-sep-2026. **Y no son la misma enfermedad**: las **cinco** primeras llevan listas **CORTAS** y hacen desaparecer registros (el desplegable del buscador, además, no ofrece `vencido` ni `expirado`, así que los 9 viajes colgados salen en la pestaña pero **no se pueden buscar**). La de `functions/index.js` está **COMPLETA**: no esconde nada, es un **GEMELO** (SEGUNDA LEY) que hoy dice lo mismo y mañana no. La lista buena es `ESTADOS_TERMINADOS`, pero **no todas pueden importarla**: las 2 de `guajirago/src/` sí (misma carpeta), y ahí el gemelo no se ata — **se borra**. Las 3 del panel son **otro repo** y `functions/` es **otro paquete npm con su propio `node_modules`**: ahí la copia se queda y **hay que ATARLA**, como se hizo en `Viajes.js` (`NO_COMPLETADOS` + amarre). **Se van una a una, cada una con sus 12 pasos** (decisión del dueño, 13-sep-2026) |
| 🔴 Cifras de esta tabla que ningún guion vuelve a contar | `CLAUDE.md`: la fila del WhatsApp («8 sitios»), la de la paleta muerta («~1.800 veces»), la de las fechas («74 sitios»), y la de la tarifa bajo los cancelados («~20 → ~60») | casi todas las cifras de aquí las repite un `node scripts/medir-*.cjs`, y por eso se sabe si se quedaron viejas. Éstas no: se contaron a mano una vez y **nadie escribió con qué**, que es lo que las hace irrepetibles. Recontadas el 13-sep-2026, **dos están desviadas**, y las dos veces por no decir DÓNDE se cuenta: · las fechas son **79 apariciones en 30 archivos** con `find . -name "*.js" -not -path "*/node_modules/*" -not -path "*/build/*" -not -path "./guajirago/guajirago/*" \| xargs grep -c "new Date().toISOString()"` — el «74» sale de otro recorte, y sin el `-not -path` de la copia anidada `guajirago/guajirago/` sale otro número distinto; · los colores a mano son **3.415** con `grep -rhoE "#[0-9A-Fa-f]{6}" guajirago/src guajirago-admin/src guajirago-aliados/src \| wc -l`, no «~1.800» — ése era el conteo de **una sola** carpeta. Un número sin su comando no se puede recontar, y el que no se puede recontar es el que se queda viejo sin que nadie se entere: es la misma enfermedad por la que hizo falta `scripts/medir-citas.cjs`. **No se corrigen de paso**: son filas de trabajos de otros días (PRIMERA LEY). Cada una se recuenta, con su comando escrito al lado, cuando le toque su arreglo |
| ~~El historial del conductor pedía 50 viajes sin decir por cuál empezar~~ **CERRADA (solo `AppConductor.js`)** | — | `limit(50)` a pelo: el servidor mandaba 50 **cualesquiera** y el orden por fecha se hacía **después**, en el teléfono, sobre una lista ya recortada. Con menos de 50 no se notaba; al pasar, faltan viajes **sin que nada avise**. Medido el 15-sep-2026 con `node scripts/medir-tope-historial.cjs`: el conductor con más iba por **47 de 50**. Arreglado con `orderBy('fechaSolicitud', 'desc')` — ahora son **los últimos 50**, no 50 cualesquiera. **Desplegado y verificado ese día**, en este orden: el índice desde la RAÍZ (`guajirago/firebase.json` no tiene sección `firestore`), **esperar a que estuviera construido** —el servidor contestó literalmente «that index is currently building and cannot be used yet», o sea que publicar la app en esa ventana habría dejado el historial en blanco—, comprobar con el guion que la consulta ordenada «ya funciona», y solo entonces la app desde dentro de `guajirago/`. El paquete publicado se bajó y es byte a byte el del commit. 🔴 **Y el archivo de índices se volvió a bajar DESPUÉS**: el servidor le añade `__name__ DESCENDING` y `density` por su cuenta, así que el repo y el servidor dejaban de decir lo mismo y el siguiente despliegue habría ofrecido **borrar y recrear** el índice. Lo avisó la segunda opinión. Lo vigilan dos amarres: «ninguna consulta con tope elige a ciegas» y «el índice que pide el historial está declarado» (campo, **dirección** y **alcance**: la versión que solo miraba nombres la burló la segunda opinión tres veces, dejando el historial en blanco con todo en verde) |
| Tres consultas más piden un tope sin decir por cuál empezar | `guajirago/src/Home.js` (1: `pasajeroId`) y `MisViajes.js` (2: una por `pasajeroId` y otra por `conductorId`) | el mismo `limit(50)` a pelo que se arregló en `AppConductor.js` el 15-sep-2026. Hoy no falta nada porque nadie pasa de 50, pero el día que pase, el servidor elegirá qué se ve. **No se tocaron: no estaban en la foto** (PRIMERA LEY). Y no se pueden olvidar: están en la lista `PENDIENTES` de `scripts/medir-tope-historial.cjs`, que el amarre «ninguna consulta con tope elige a ciegas» **cuenta una por una** — si aparece una cuarta se pone rojo, y si se arregla una y nadie baja la cuenta, también. Son los MISMOS archivos que la deuda de la lista de estados corta, así que los dos fallos se arreglan juntos cuando les toque. **Ojo a los índices**: la de `MisViajes.js` que filtra por `conductorId` usará **el mismo índice** que se crea hoy; las dos de `pasajeroId` pedirán otro (`pasajeroId` + `fechaSolicitud`), y el índice va antes que la app |
| 🔴 Si el índice falta, el historial del conductor se queda EN BLANCO y no lo dice nadie | `guajirago/src/AppConductor.js`, el `catch (e) { console.error(e); }` de `HistorialConductor` | desde el 15-sep-2026 la consulta puede fallar por algo nuevo: que el índice no esté puesto, o que se esté construyendo. Cuando falla, `getDocs` revienta, `setViajes` no se llama y el conductor ve **«Mis viajes» vacío y «GANANCIAS DE HOY» en cero**, sin error, sin ventanita, sin nada. Va contra la REGLA 9 (nada se rechaza en silencio) y contra la regla de que los avisos son ventanitas. **Antes de este arreglo esa consulta no podía fallar así**; ahora sí, y de forma segura durante toda la ventana del despliegue. Lo cazó la segunda opinión. **No se arregló de paso** (PRIMERA LEY): el arreglo del día era el tope. Es un `catch` callado más, hermano de los 7 ya anotados de `Solicitar.js` |
| Un aviso del medidor del historial que se quedó viejo el mismo día | `scripts/medir-historial-conductor.cjs`, el aviso «⚠ N conductor(es) rozando el tope» | dice que al pasar del tope «el servidor elige cuáles se caen —la consulta no dice por cuál empezar—». **Eso dejó de ser cierto el 15-sep-2026**: desde que la consulta ordena, pasar de 50 significa que se caen **los más viejos, de forma predecible**, no que el servidor elija. El aviso sigue siendo útil —al conductor le faltarán viajes igual— pero el motivo que da ya no es el bueno. **No se tocó: ese archivo no estaba en la foto** (PRIMERA LEY). Es exactamente lo que persigue `scripts/medir-citas.cjs`, pero una nota que miente sin citar ningún renglón, así que ese guion no la ve |
| La tarifa se sigue enseñando bajo los viajes que no se cobraron | `guajirago/src/AppConductor.js`, el `{v.tarifa}` de la tarjeta de `HistorialConductor` | el 13-sep-2026 se arregló que **salieran** los cancelados y colgados, y que **dijeran en palabras** qué pasó en vez de pintarse verde «Completado». Lo que NO se tocó: debajo de cada tarjeta sigue el número de la tarifa, en negro y grande, también en las que dicen «Lo canceló el cliente». Antes eran ~20 tarjetas, ahora son ~60. Que ahí ponga la tarifa, o un guion, o nada, es **decisión del dueño** — y por la PRIMERA LEY no se arregló de paso |
| Otra lista corta en la misma pantalla, en el camino del viaje vivo | `guajirago/src/AppConductor.js`, dentro de `agregarViajeEscuchando` | `if (data.estado === 'cancelado' \|\| data.estado === 'cancelado_conductor')` — le faltan `vencido` y `expirado`, los dos que escribe la rutina del servidor. Si un viaje se cuelga y el servidor lo expira mientras el conductor tiene la app abierta, esa rama **no se entera**. NO es la lista del historial (esa ya sale de `ESTADOS_TERMINADOS`): es otra, en el camino del viaje en curso. Apareció midiendo el historial el 13-sep-2026 y **no estaba en la foto** |
| Al `expirado` le falta el porqué, también en la app del conductor | `guajirago/src/AppConductor.js` (el `razonCancelacion` de la tarjeta de `HistorialConductor`) ↔ `guajirago/functions/index.js` | la tarjeta enseña `razonCancelacion`, y la rutina que cierra los colgados escribe el suyo en **`motivoExpiracion`**. Es el MISMO fallo ya anotado para `guajirago-admin/src/Mensajeria.js`, así que son **dos pantallas** las que enseñan «Quedó sin terminar» sin poder decir por qué, con el dato guardado al lado |
| ~~El historial del conductor no veía 40 viajes suyos — y los 40 habrían salido en verde~~ **CERRADA** | — | `AppConductor.js` filtraba `finalizado \|\| cancelado`, así que los que cancelaba él mismo y los colgados **no aparecían en su historial**: sin error, sin rojo, no estaban. Medido con `node scripts/medir-historial-conductor.cjs` **contra el código de antes** (el guion lo sabe hacer, para que el paso 1 y el paso 12 se puedan carear): **40 viajes invisibles entre 5 conductores**; `XfXCz3rgYSXC` veía **16 de sus 47**. Y una SEGUNDA MITAD que iba junta: una bandera decidía si el viaje salía **verde «Completado» con su tarifa debajo**, y solo conocía `cancelado` — así que arreglar solo el filtro habría metido esos 40 y **los habría pintado verdes a los 40**, porque ninguno es `finalizado` ni `cancelado`. Peor que el fallo original. Arreglado: la lista se **importa** de `estadosViaje.js`, la bandera es `!== 'finalizado'` (completado es UNO), y cada final dice en palabras qué pasó. Ahora: **5 de 5 conductores ven todos los suyos, 0 en verde sin serlo** |
| 🔴 **LA LECCIÓN DE ESE ARREGLO: el medidor SUPONÍA** | `scripts/medir-historial-conductor.cjs` ↔ `pruebas/amarres.test.js` | la vigilancia hubo que rehacerla **tres veces**, y las tres la burló la segunda opinión con el fallo entero puesto y **todo en verde**. (1) Leía el primer `.filter` del ARCHIVO, no el de `HistorialConductor`: un señuelo cuatro renglones más arriba bastaba. (2) Seis formas más de esconder viajes o pintarlos de verde sin que nada chistara. (3) Y el de fondo: **el guion no medía, copiaba**. Daba por buena la lista en cuanto veía el NOMBRE escrito, así que el informe restaba la lista buena de sí misma —«FALTAN» no podía dar otra cosa que 0— y las dos comparaciones del amarre comparaban `ESTADOS_TERMINADOS` consigo mismo: **no podían ponerse rojas nunca**. Hoy: el guion **saca el filtro y la bandera del archivo y los CORRE**, estado por estado; el recorrido vive **una sola vez** (el amarre **importa** el guion, no lo copia); el tope del `limit` entra en el veredicto, porque antes el guion avisaba «⚠ rozando el tope» y dos renglones más abajo firmaba «✓ todos ven todo»; y hay una prueba —«**y el medidor del historial no se puede ablandar**»— que le da de comer al propio lector **28 pantallas de mentira** en memoria y exige que se queje de todas, porque ablandar el lector era el escape que quedaba: el amarre se cree lo que él le diga, y el guardián lo aprueba porque está en la foto. Sus anclas **salen del propio archivo**, no copiadas — con el texto a mano, unas comillas dobles de Prettier la ponían roja acusando al medidor |
| Dos cosas que el medidor del historial todavía no mira | `scripts/medir-historial-conductor.cjs` | el lector prueba el filtro de la pantalla dándole solo `{ estado }`, así que una condición sobre OTRO campo —`.filter(v => ESTADOS_TERMINADOS.includes(v.estado) && !v.oculto)`— le pasa por delante sin que chiste. Y de la consulta solo vigila `where('estado')`: un `where('fechaSolicitud', '>=', ...)` escondería viajes igual de bien, y es el hermano del `limit` que sí entró al veredicto. Ninguna de las dos está puesta hoy en la pantalla; son huecos del medidor, no fallos del conductor. Los encontró la segunda opinión el 13-sep-2026 |
| 🔴 Este arreglo corrió hacia abajo ~33 renglones de `AppConductor.js`, y hay notas que lo citan por número | **36 citas en 8 archivos**: `guajirago/src/estadosViaje.js` (`:966`, `:980`, `:1006`) · `firestore.rules` · `firestore.rules.LIVE` · `guajirago/functions/index.js` · `pruebas/reglas.test.js` · `pruebas/viajeActivo.test.js` · `pruebas/viajesColgados.test.js` · `scripts/medir-panico.cjs` | las tres de `estadosViaje.js` eran **exactas** hasta hoy (`setFase('recogiendo')`, `fase: 'en_punto'`, `fase: 'en_viaje'`) y ahora apuntan a otra cosa; el código está en **999, 1013 y 1039**. 🔴 **Y `scripts/medir-citas.cjs` NO lo ve**: comprueba que el renglón EXISTA, no que diga lo que la nota promete — o sea que su «✓ ninguna nota apunta a algo que no existe» es cierto y aun así estas notas mienten. Lo cazó la segunda opinión; varias ya estaban viejas de antes de hoy. **No se tocan**: son 8 archivos fuera de la foto (PRIMERA LEY). Es la prueba a favor de la regla que el propio guion predica: **se cita el nombre de la función, que no se mueve**, no el número de renglón |
| Dos copias de la misma lista en el panel | `guajirago-admin/src/Viajes.js` (`NO_COMPLETADOS`) y `Mensajeria.js` (`esCancelado`) | la misma lista de cuatro finales, escrita a mano dos veces en el MISMO repo. Las dos están amarradas a `ESTADOS_TERMINADOS`, así que no pueden separarse en silencio — pero siguen siendo dos, y el panel podría tener un archivo propio en vez de repetirla |
| ~~Los 9 viajes `expirado` no salían en ninguna pestaña~~ **CERRADA (solo `Viajes.js`)** | — | «en curso» consulta `esperando`/`aceptado`, «completados» filtra `finalizado`, y «cancelados» solo conocía `cancelado` y `cancelado_conductor`. Los **9 viajes en `expirado`** —los que el conductor aceptó y dejó a medias— **desaparecían del panel**: no daban error, no salían en rojo, no estaban. Y el porcentaje de cancelación salía **80% cuando era 82%**, calculado sobre una lista a la que le faltaban esos nueve. Arreglado el 12-sep-2026: una sola lista `NO_COMPLETADOS` (= `ESTADOS_TERMINADOS` menos `finalizado`) que usan **la pestaña y la estadística**, y `vencido`/`expirado` con su nombre en cristiano. Lo vigila el amarre «todos los estados caben en alguna pestaña del panel» y lo cuenta `node scripts/medir-pestanas-viajes.cjs` (ahora: 91 de 91 con pestaña) |
| El historial del pasajero se deja 40 viajes fuera | `guajirago/src/Home.js` y `MisViajes.js` | «Mis viajes» filtra `finalizado \|\| cancelado`, así que los `cancelado_conductor` (31), `expirado` (9) y `vencido` **no aparecen nunca**. El pasajero no ve los viajes que le cancelaron o que se quedaron colgados. En `MisViajes.js` esa lista corta está **DOS veces** (la primera filtra, ordena y **tira el resultado**: es código muerto), así que al arreglarlo hay que mirar las dos y no dar por hecho que es un solo renglón |
| Una CUARTA lista de «en curso» en la misma pantalla | `guajirago-admin/src/Mensajeria.js`, dentro de `etiquetaEstado` | `etiquetaEstado` repite `esperando` y `aceptado` a mano, aparte de `esEnCurso` y de `ESTADOS_EN_CURSO`. El amarre ata `esEnCurso`; si mañana entra un estado en curso nuevo, la etiqueta lo pintará gris aunque el conteo lo cuente bien |
| Los `expirado` se quedan sin motivo en pantalla | `guajirago-admin/src/Mensajeria.js` (la tarjeta) ↔ `guajirago/functions/index.js` | la tarjeta enseña `razonCancelacion`, y la rutina que cierra los colgados escribe el suyo en **`motivoExpiracion`**. Así que de un mandado `expirado` se ve QUÉ pasó (la etiqueta) pero no POR QUÉ, aunque el dato esté guardado |
| Un TERCER conteo de «en curso», sin vigilar | `guajirago-admin/src/App.js`, el `enCurso` de las estadísticas | mira `v.fase === 'en_viaje' \|\| v.fase === 'en_punto'` **sin comprobar que el viaje esté vivo** — el mismo patrón que hacía que el botón de pánico mandara los datos de un conductor de un viaje terminado. Hoy no da un número malo porque lo tapa el filtro de «última hora» y los datos son de julio. El amarre del panel cubre `Viajes.js` y `Mensajeria.js`, **no éste** |
| ~~El conteo «en curso» de mensajería~~ **CERRADA** | — | `guajirago-admin/src/Mensajeria.js` llevaba una TERCERA copia de la lista de estados en curso, con `'en_viaje'` dentro. Medido el 12-sep-2026: **0 viajes con `estado: 'en_viaje'`** y 4 con `fase: 'en_viaje'` — así que ese tercero **no sumaba nada**; lo que había era una copia que nadie vigilaba y una palabra que hacía creer que una fase es un estado. Ya sale de `ESTADOS_EN_CURSO`, y el amarre «el panel y la app dicen lo mismo» cubre ahora también `Mensajeria.js`. (Esta fila decía además «le falta `en_negociacion`»: ese estado se retiró ese mismo día, o sea que mandaba a resucitar un muerto.) |
| El libro de excepciones del guardián para el trabajo al escribir en él | `scripts/guardian.cjs` ↔ `.guardian-excepciones.log` | la ley manda anotar en `.guardian-excepciones.log` cada vez que hay que salirse de la lista declarada... y **escribir ahí es salirse de la lista**: el guardián lo cuenta como archivo no declarado y para el trabajo. Pasó el 15-sep-2026 al pedir permiso para `firestore.indexes.json`. No es un falso positivo tonto —el guardián mira bien—, es que **su propio libro debería estar siempre permitido**. Se apaña declarándolo en la foto, pero entonces hay que acordarse cada vez. **Es un arreglo de una línea en `scripts/guardian.cjs`, que no estaba en la foto**: va aparte, con sus 12 pasos |
| package-lock desincronizados | `guajirago/`, `guajirago-admin/` | declaran firebase pero el lock no lo trae: `npm ci` falla hasta el próximo `npm install` (su lock se commitea aparte) |
| Copia huérfana del empleado | `restaurantes/{id}/empleados/JUAN` | inofensiva; borrar registros está prohibido (REGLA 12 / lápidas) |

---

## Cómo se trabaja (leyes de siempre)

1. **Un arreglo a la vez.** Un arreglo = un commit = una verificación.
2. **Se re-audita lo arreglado.** "Audita, prueba y VUELVE a auditar."
3. **NADA sin pedirse.** Si se detecta una necesidad: se PROPONE, no se construye.
4. **Nada se deja para después.** O se hace, o se anota y se le dice al dueño.
5. **Un proceso, un solo sitio.** Antes de escribir algo nuevo: buscar si ya existe.
6. **Se reporta en cristiano.** Palabras simples; la jerga solo en los commits.
7. **Al citar una regla se dice qué dice.**
8. **Que lo diga la app, no yo.** El careo se construye en la pantalla del dueño.
9. **Nada se rechaza en silencio.** Todo rechazo va a una bandeja con su motivo.
10. **Los borrados dejan lápida.** Un registro solo se borra con su marca de borrado.

---

## 🗺️ EL TERRENO DE GUAJIRAGO (leer antes de cualquier despliegue)

**Tres apps, tres repos de GitHub, UN solo proyecto Firebase (`guajirago`).**
Todo el acoplamiento entre apps es contrato implícito de campos en Firestore: no hay API.
Cambiar un campo en una app puede romper otra sin que nada avise.

| Carpeta | Repo GitHub | Qué es | Hosting |
|---|---|---|---|
| `guajirago/` | `frey2605/guajirago` | app de pasajero y conductor (taxi, mototaxi, mensajería) | target por defecto |
| `guajirago-admin/` | `frey2605/guajirago-admin` | panel de administración y superadmin | target `admin` |
| `guajirago-aliados/` | `frey2605/guajirago-aliados` | restaurantes y agencias de turismo | — |

**`guajirago-admin/` y `guajirago-aliados/` están en `.gitignore` del repo raíz y son repos
APARTE.** Un `git status` en la raíz NO ve sus cambios. El guardián los mira por separado.

**Las Cloud Functions** (`guajirago/functions/index.js`) corren con el SDK admin y **se saltan
las reglas de Firestore**. Lo que se blinde en las reglas hay que moverlo allí, no borrarlo.

---

## 🪤 TRAMPAS DE ESTE REPO

> Esta sección se llena sola a medida que las trampas muerdan. Cada una con fecha.

- **(23-ago-2026) Ya solo queda UNA copia de trabajo.** La buena y única es
  `C:\Users\Windows 11\GuajiraGo` (rama `main`). La de `Dropbox\PROYECTOS\GuajiraGo` tenía
  **99 archivos borrados** del árbol de trabajo — un `git commit -a` allí borraba el proyecto —
  y **se borró el 23-ago-2026**, tras comprobar objeto por objeto que no contenía nada que no
  estuviera ya en la copia buena. Su `git branch -vv` decía *"ahead 11"*: era **mentira**,
  comparaba contra un `origin/main` viejo que nunca refrescó.
- **(23-ago-2026) Dentro de la copia buena hay otro repo anidado** en `guajirago/`, rama `v2.1`
  (HEAD `12a0de9`, 6-jul). No contiene el trabajo de taxi de julio: **no se trabaja desde ahí**.
  · **(5-sep-2026, medido) Ese HEAD SÍ está en GitHub.** Esta trampa decía que ni él ni sus
    3 stashes estaban, y era **falso**: `12a0de9` es exactamente la rama `respaldo-v2.1-23ago`.
    Alguien lo subió el 23-ago y nadie corrigió el aviso. Se comprobó con `git ls-remote`.
  · **De los 3 stashes, DOS ya están guardados** (5-sep-2026): `respaldo-stash-1-estable-antes-v2.0`
    y `respaldo-stash-2-splash-original`.
  · **El tercero, `stash@{0}`, NO se puede subir: lleva una credencial dentro.** GitHub lo
    rechaza por protección de secretos — un *Twilio Account SID* en `functions/index.js:137`.
    **No se fuerza**: forzarlo publicaría la credencial. Comprobado el 5-sep-2026: esa
    credencial **NO está** en `main`, ni en `v2.1`, ni en `respaldo-v2.1-23ago`, ni en
    `rediseno-tema-claro`, ni en ninguno de los 14 commits del historial de `functions/index.js`
    — y el código de HOY no la tiene. O sea: **vive solo en ese stash, en este disco**.
    Para guardarlo hay que rotar antes esa credencial en Twilio, o quitarla del stash.
- **(15-sep-2026) Los índices de Firestore tampoco estaban en el repo.** Vivían solo en la
  consola, sin historial — la misma trampa que las reglas, un mes después. Y peor: el
  despliegue de índices **SINCRONIZA**, así que un `firestore.indexes.json` hecho a mano al que
  le falte uno de los que ya hay **ofrece borrarlo**, y un índice borrado no da error de
  despliegue: hace que la consulta que lo usaba falle en la cara del usuario. Se bajan con
  `node scripts/bajar-indices.cjs` ANTES de tocar el archivo.
  · **Y el formato NO se convierte a mano.** La primera versión de ese guion leía la API de
    Firestore y traducía la respuesta por su cuenta: le quitaba el `__name__`, le faltaba el
    `density`, y se inventaba una «excepción de campo» `__default__`/`*` que **no es una
    excepción** —es la configuración por defecto de la base—, así que el despliegue habría
    intentado cambiar cómo se indexa TODO. Lo cazó el careo contra
    `npx firebase firestore:indexes`, que es quien sabe el formato. Ahora el guion le pregunta
    a él. SEGUNDA LEY aplicada a un formato: el que sabe cómo se escribe un archivo es la
    herramienta que luego lo lee.
- **(23-ago-2026) Las reglas de Firestore no estaban en el repo.** Vivían solo en la consola,
  sin historial. La copia bajada del servidor quedó en `firestore.rules.LIVE`.
- **(23-ago-2026) `firebase` no tiene comando para LEER reglas.** Se bajan con la API
  `firebaserules.googleapis.com` usando la sesión que el CLI ya tiene abierta.

---

## 🗄️ EL RESPALDO — regla del dueño

> **Palabras del dueño:** «GitHub es el respaldo principal.»
> «Si el proyecto se pone en Dropbox hay que excluir `.git` y `node_modules` de la
> sincronización. Dropbox ya se comió una copia entera del código.»

1. **El respaldo es GitHub, y solo GitHub.** Una carpeta en el disco no es respaldo. Una
   carpeta en Dropbox tampoco: es una copia que alguien más puede mover o borrar.
2. **Lo que no está empujado a GitHub, NO está respaldado.** Da igual que esté commiteado:
   un commit que solo vive en esta máquina se pierde con la máquina.
3. **Si el proyecto se pone en Dropbox, se excluyen de la sincronización `.git` y
   `node_modules`.** El `.git` porque Dropbox lo sincroniza a medias y lo corrompe;
   `node_modules` porque son miles de archivos que solo sirven para atascar la sincronización.
4. **Los tres repos se dejan al día.** Raíz, `guajirago-admin` y `guajirago-aliados` son
   repos APARTE: empujar uno no empuja los otros. Se comprueba contra el servidor
   (`git ls-remote`), no contra el `origin/main` local, que puede estar viejo.

**Lo que se midió el 23-ago-2026** (por eso esta regla existe): la copia de
`Dropbox\PROYECTOS\GuajiraGo` tenía **99 archivos borrados** del árbol de trabajo. Se salvó
el código porque ya estaba en GitHub — no porque Dropbox lo guardara. Y su `git branch -vv`
decía *"ahead 11"*, que era **mentira**: comparaba contra un `origin/main` viejo que nunca
refrescó. Los 11 commits ya estaban en GitHub.
